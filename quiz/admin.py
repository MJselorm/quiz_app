from django.contrib import admin
from django.contrib import admin
from .models import Question  # import your model

admin.site.register(Question)  # register it so it shows in admin

# Register your models here.
